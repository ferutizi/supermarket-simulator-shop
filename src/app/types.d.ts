export type Product = {
  id: number,
  name: string,
  supplier: string,
  placeAt: string,
  totalPrice: number,
  unityPrice: number,
  quantityPerBox: number,
  img: string,
  amount: number,
  subTotal: number,
}